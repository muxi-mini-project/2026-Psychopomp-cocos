# 修改记录

此文档记录每次代码修改，便于后续 review。

---

## 2026-03-21

### DataManager.ts - 修改项 1：startNewGame() 不立即保存

**修改内容**：
```diff
public startNewGame(): void {
    this._saveData = {
        saveId: "new_game",
        saveTime: Date.now(),
        gameVersion: GAME_VERSION,
-       currentScene: "scene_intro",
+       currentScene: "",
        storyFlags: {},
        inventory: [],
        visitedScenes: []
    };

    console.log("[DataManager] 新游戏已初始化");
-   this.saveGame(AUTO_SAVE_SLOT, true);
}
```

**理由**：存档应在开场演出完成后由 GameManager._enterGame() 统一保存，而非新游戏初始化时立即保存。currentScene 设为空字符串，因为开场阶段无实际场景。

---

### ResourceManager.ts - 修改项 1：简化 init()

**修改内容**：
```diff
public init(onComplete: () => void): void {
-   this._loadProgress = 0;
-   this._isLoading = true;
-
-   resources.loadDir('data', (err, assets) => {
-       if (err) {
-           console.error("[ResourceManager] 核心配置加载失败", err);
-           this._isLoading = false;
-           return;
-       }
-
-       this._loadProgress = 1;
-       this._isLoading = false;
-       console.log("[ResourceManager] 核心配置加载完成");
-
-       if (onComplete) {
-           onComplete();
-       }
-   });
+   console.log("[ResourceManager] 初始化完成");
+   if (onComplete) {
+       onComplete();
+   }
}
```

**理由**：data 目录加载后未使用，DataManager 已有独立配置加载逻辑。

---

### SceneViewManager.ts - 修改项 1：initializeFromSave() 处理空场景

**修改内容**：
```diff
public initializeFromSave(): void {
    const currentScene = DataManager.instance.getCurrentScene();
+   if (!currentScene || currentScene === "") {
+       console.log("[SceneViewManager] 当前无场景（开场阶段）");
+       return;
+   }
    this.loadScene(currentScene);
    this.preloadNextScenes();
}
```

**理由**：开场阶段 currentScene 为空，不应尝试加载场景。

---

### SceneViewManager.ts - 修改项 2：loadScene() 更新 DataManager.currentScene

**修改内容**：
```diff
this._currentSceneId = sceneId;
+ DataManager.instance.setCurrentScene(sceneId);
```

**理由**：场景切换时应同步更新 DataManager 中的当前场景 ID。

---

### SceneViewManager.ts - 修改项 3：预加载多场景（使用 Map）

**修改内容**：
```diff
- private _preloadedSceneId: string | null = null;
+ private _preloadedSceneMap: Map<string, Node> = new Map();

public preloadSceneForSwitch(sceneId: string): void {
-   if (this._preloadedSceneId === sceneId) return;
-   ResourceManager.instance.loadScene(sceneId).then((prefab) => {
-       this._clearNextScene();
-       const node = instantiate(prefab);
-       node.name = sceneId;
-       this.nextSceneNode.addChild(node);
-       this.nextSceneNode.active = false;
-       this._preloadedSceneId = sceneId;
-   });
+   if (this._preloadedSceneMap.has(sceneId)) return;
+   ResourceManager.instance.loadScene(sceneId).then((prefab) => {
+       const node = instantiate(prefab);
+       node.name = sceneId;
+       this._preloadedSceneMap.set(sceneId, node);
+   });
}

public preloadNextScenes(): void {
    const config = DataManager.instance.getSceneConfig(this._currentSceneId);
    const preloadList = config?.preloadNext || [];
+   this._preloadedSceneMap.clear();
-   preloadList.forEach((sceneId: string) => {
-       if (sceneId !== this._preloadedSceneId) {
-           this.preloadSceneForSwitch(sceneId);
-       }
-   });
+   preloadList.forEach((sceneId: string) => {
+       this.preloadSceneForSwitch(sceneId);
+   });
}

public switchToScene(sceneId: string): void {
-   public switchToScene(sceneId: string, transitionType: string = "fade"): void {
    // ...
    if (this._preloadedSceneId !== sceneId) {
-       this.preloadSceneForSwitch(sceneId);
+       ResourceManager.instance.loadScene(sceneId).then((prefab) => {
+           node = instantiate(prefab);
+           node.name = sceneId;
+           this._doSwitch(sceneId, node);
+       });
    }
    // ...
}

- private _completeSwitch() // 已删除
- private _doTransitionOut() // 已删除
- private _doTransitionIn() // 已删除
- private _clearNextScene() // 已删除
+ private _doSwitch(sceneId: string, node: Node): void { ... }
```

**理由**：
1. 使用 Map 替代单值存储，支持多场景预加载
2. 移除场景切换过渡动画（无需 transitionType）
3. 删除不再使用的 nextSceneNode 和 transitionNode 相关代码

---

### InteractableManager.ts - 修改项 1：switchScene 自动执行

**修改内容**：
```diff
// 切换场景
if (result.switchScene) {
    triggerResult.switchedScene = result.switchScene;
+   SceneViewManager.instance.switchToScene(result.switchScene);
}
```

**理由**：场景切换应直接执行，而非仅存储到 result。

---

### InteractableManager.ts - 修改项 2：添加 ADD_ITEM_REQUEST 监听

**修改内容**：
```diff
onLoad() {
    director.on("INTERACTABLE_CLICK", this._onInteractableClick, this);
    director.on("SET_FLAG_REQUEST", this._onSetFlagRequest, this);
+   director.on("ADD_ITEM_REQUEST", this._onAddItemRequest, this);
}

+ private _onAddItemRequest(data: { itemId: string }): void {
+     if (data?.itemId) {
+         DataManager.instance.addItem(data.itemId);
+     }
+ }

onDestroy(): void {
    director.off("INTERACTABLE_CLICK", this._onInteractableClick, this);
    director.off("SET_FLAG_REQUEST", this._onSetFlagRequest, this);
+   director.off("ADD_ITEM_REQUEST", this._onAddItemRequest, this);
}
```

**理由**：为复杂交互场景预留的灵活性，场景脚本处理交互过程后通过事件通知添加物品。

---

### DialogManager.ts - 修改项 1：添加 DIALOG_NEXT 监听

**修改内容**：
```diff
onLoad() {
    // ...
    director.on("DIALOGUE_REQUEST", this._onDialogueRequest, this);
+   director.on("DIALOG_NEXT", this._onDialogNext, this);
}

+ private _onDialogNext(): void {
+     this.nextLine();
+ }

protected onDestroy(): void {
    director.off("DIALOGUE_REQUEST", this._onDialogueRequest, this);
+   director.off("DIALOG_NEXT", this._onDialogNext, this);
    // ...
}
```

**理由**：外部（DialogPanel）点击时发出 DIALOG_NEXT 事件推进对话。

---

### DialogManager.ts - 修改项 2：_displayLine() 添加 lines 数组校验

**修改内容**：
```diff
private _displayLine(): void {
+   if (!this._currentDialogue?.lines?.[this._currentLineIndex]) {
+       console.warn("[DialogManager] 对话行不存在");
+       this._finishDialogue();
+       return;
+   }
    const line = this._currentDialogue.lines[this._currentLineIndex];
    director.emit("DIALOGUE_LINE", {
        text: line.text,
        index: this._currentLineIndex,
        total: this._currentDialogue.lines.length
    });
}
```

**理由**：避免对话配置异常时报 Cannot read property 'text' of undefined。

---

### InventoryManager.ts - 修改项 1：物品消耗后同步取消选中状态

**修改内容**：
```diff
onLoad() {
    if (InventoryManager._instance) {
        this.node.destroy();
        return;
    }
    InventoryManager._instance = this;
    director.addPersistRootNode(this.node);
+
+   director.on("ITEM_REMOVED", this._onItemRemoved, this);
}

+ private _onItemRemoved(itemId: string): void {
+     if (this._selectedItem === itemId) {
+         this._selectedItem = null;
+         director.emit("ITEM_DESELECTED");
+     }
+ }

onDestroy() {
+   director.off("ITEM_REMOVED", this._onItemRemoved, this);
    this._selectedItem = null;
}
```

**理由**：物品被消耗时，如果与当前选中物品相同，需要自动取消选中状态。

---


**修改内容**：
```diff
- public loadConfig<T>(path: string, onComplete: (data: T) => void): void {
-     resources.load(`data/${path}`, JsonAsset, (err, asset) => {
-         if (err) {
-             console.error(`[ResourceManager] 配置加载失败: ${path}`, err);
-             return;
-         }
-         onComplete(asset.json as T);
-     });
- }
```

**理由**：该方法无人调用，DataManager 直接调用 resources.load() 加载配置。

---


**注意**：表示"intro结束"的 flag 可能有多个，具体作用需确认：
- `INTRO_PLAYED` - 用于标记开场演出是否完成
- `INTRO_CUTSCENE_PLAYED` - 用于标记开场对话演出是否完成（见 GameManager._onIntroCutsceneComplete）

**统一前必须确认每个 flag 的具体作用，避免错误统一。**

---

### UIManager.ts - 修改项 1：修复 onVideoEnded() 事件名

**修改内容**：
```diff
public onVideoEnded(): void {
-   director.emit("CUTSCENE_END");
+   director.emit("INTRO_COMPLETE");
}
```

**理由**：开场视频结束后应发出 INTRO_COMPLETE 事件，通知 GameManager 进入开场对话阶段。

---

### UIManager.ts - 修改项 2：修复 updateInventoryUI() 事件名和冗余逻辑

**修改内容**：
```diff
public updateInventoryUI(): void {
    if (this.inventoryPanel) {
-       this.inventoryPanel.active = true;
-       director.emit("INVENTORY_REFRESH");
+       director.emit("INVENTORY_UPDATE");
    }
}
```

**理由**：
1. 事件名应为 INVENTORY_UPDATE（与监听名称一致）
2. inventoryPanel.active 状态应由 Panel 组件自己管理

---

### UIManager.ts - 修改项 3：删除未使用的私有方法

**修改内容**：
```diff
- private _showInventoryPanel(): void {
-     if (this.inventoryPanel) {
-         this.inventoryPanel.active = true;
-     }
- }
-
- private _showMenuPanel(): void {
-     if (this.menuPanel) {
-         this.menuPanel.active = true;
-     }
- }
```

**理由**：这两个方法声明后从未被调用，属于死代码。

---

### GameManager.ts - 修改项 1：简化 initializeGame() 和删除 _startNewGame()

**修改内容**：
```diff
public initializeGame(): void {
    const loaded = DataManager.instance.loadGame("auto_save");

-   if (!loaded) {
-       // 无存档 → 新游戏
-       this._startNewGame();
-   } else {
-       // 有存档 → 检查动画状态
-       this._resumeGame();
-   }
+   if (!loaded) {
+       return;
+   }
+   this._resumeGame();
}

- private _startNewGame(): void {
-     DataManager.instance.startNewGame();
-     director.emit("INTRO_START");
- }
```

**理由**：
1. `_startNewGame()` 与 `_onStartNewGame()` 功能重复
2. `initializeGame()` 只负责加载存档，不负责开始新游戏
3. 无存档时直接返回，让主菜单显示"新游戏"选项

---

### GameManager.ts - 修改项 2：_resumeGame() 改为内部方法

**修改内容**：
```diff
- private _resumeGame(): void {
-     const introPlayed = DataManager.instance.getIntroPlayed();
-     const endingPlayed = DataManager.instance.getEndingPlayed();
-
-     if (!introPlayed) {
-         // 开场动画未完整播放，重新播放
-         director.emit("INTRO_START");
-     } else if (endingPlayed) {
-         // 结局已播放，回主菜单
-         director.emit("SHOW_MAIN_MENU");
-     } else {
-         // 正常恢复游戏
-         SceneViewManager.instance.initializeFromSave();
-     }
- }
+ private _resumeGame(): void {
+     const introPlayed = DataManager.instance.getIntroPlayed();
+     const endingPlayed = DataManager.instance.getEndingPlayed();
+
+     if (!introPlayed) {
+         director.emit("INTRO_START");
+     } else if (endingPlayed) {
+         director.emit("SHOW_MAIN_MENU");
+     } else {
+         this._enterGame();
+     }
+ }
```

**理由**：统一恢复游戏的入口判断逻辑。

---

### GameManager.ts - 修改项 3：_onLoadGame() 调用 _resumeGame()

**修改内容**：
```diff
private _onLoadGame(data: { slotId: string }): void {
    if (data?.slotId && DataManager.instance.loadGame(data.slotId)) {
-       SceneViewManager.instance.initializeFromSave();
+       this._resumeGame();
    }
}
```

**理由**：加载存档后应通过 `_resumeGame()` 统一处理后续流程（可能需要检查结局状态）。

---

### GameManager.ts - 修改项 4：添加 INTRO_CUTSCENE_COMPLETE 事件注销

**修改内容**：
```diff
protected onDestroy(): void {
    director.off("INTRO_COMPLETE", this._onIntroComplete, this);
    director.off("ENDING_COMPLETE", this._onEndingComplete, this);
+   director.off("INTRO_CUTSCENE_COMPLETE", this._onIntroCutsceneComplete, this);
    director.off("START_NEW_GAME", this._onStartNewGame, this);
    // ...
}
```

**理由**：onLoad 中注册了监听，onDestroy 中应正确注销。

---

### GameManager.ts - 修改项 5：修复 INTRO_CUTSCENE_COMPLETE 事件注销名

**修改内容**：
```diff
- director.off("INTRO_COMPLETE", this._onIntroCutsceneComplete, this);
+ director.off("INTRO_CUTSCENE_COMPLETE", this._onIntroCutsceneComplete, this);
```

**理由**：注销事件名与注册时不一致，会导致事件监听泄漏。

---

### ResourceManager.ts - 修改项：删除冗余字段 _loadProgress 和 _isLoading

**修改内容**：
```diff
- private _loadProgress: number = 0;
- private _isLoading: boolean = false;
  private _loadedScenes: Set<string> = new Set();
  private _loadedVideos: Set<string> = new Set();
```

同时删除 `preloadScene()` 和 `loadVideo()` 中对这些字段的读写，以及删除 `getLoadProgress()` 和 `isLoading()` 公共方法。

**理由**：这些字段在 `init()` 简化后无人使用，且 `loadScene()` 使用 Promise 不更新这些值，存在不一致。

---

### SceneViewManager.ts - 修改项：删除未使用的 @property 字段

**修改内容**：
```diff
- @property(Node)
- currentSceneNode: Node = null;
-
- @property(Node)
- nextSceneNode: Node = null;
-
- @property(Node)
- transitionNode: Node = null;
-
- private _currentSceneNode: Node = null;
+ private _currentSceneNode: Node = null;
```

同时更新 `initializeSceneNodes()` 只创建 `_currentSceneNode`，移除 `nextSceneNode` 和 `transitionNode` 的创建逻辑。

**理由**：这两个字段是旧版过渡动画系统残留，新版直接切换不使用过渡。

---
